[user]
	email = jheld135@gmail.com
	name = JMH
[push]
	default = current

[core]
	excludesfile = ~/.gitignore
	editor = vim
[alias]
	git = !exec git
	undo = checkout HEAD --
	undo-commit = reset HEAD~
	reword = commit --amend
[github]
	user = solkaz
